# Enable CSV result saving with a custom prefix
csv = rec_api_ng

# Set user count, spawn rate, and test duration
headless = true
users = 2
spawn-rate = 1
run-time = 60s

# Only print the summary report
only-summary = true
